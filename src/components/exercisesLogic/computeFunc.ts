const computeFunc = {
  'Умножение': (a: number, b: number) => a * b,
  'Простое деление': (a: number, b: number) => a / b,
  'Дробное деление': (a: number, b: number) => a / b,
  'Квадрат числа': (a: number) => a ** 2,
  'Корень квадратный': (a: number) => Math.sqrt(a),
};

export default computeFunc;
